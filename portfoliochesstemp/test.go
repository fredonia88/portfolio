package main

import (
	"fmt"
	// "strings"
	// "math"
	// "math/rand"
	// "errors"
	// "log"
)

func main() {
	
	rowCol := make([][2]int, 0, 5)

	for i := range rowCol {
		fmt.Println(i)
	}

	fmt.Println(rowCol)
}
